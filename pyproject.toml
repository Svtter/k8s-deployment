[project]
name = "rename-host"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
    "fabric>=3.2.2",
]

[dependency-groups]
dev = [
    "ruff>=0.11.7",
]
